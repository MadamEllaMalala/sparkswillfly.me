/*** VARIABLES & OBJECTS ***/
var tagMascot = document.querySelector(".u-mascot"),
	tagSparksCursor = document.querySelector(".u-sparksCursor");
/*** END VARIABLES & OBJECTS ***/

/*** EVENTS ***/
/*
document.addEventListener("mousemove", (e) => {
	var vMouseX = e.clientX;
	var vMouseY = e.clientY;
	
	tagMascot.style.transform = `translate(${vMouseX}px, ${vMouseY}px)`;
});
*/
/*** END EVENTS ***/

/*** FUNCTIONS ***/
/*** END FUNCTIONS ***/